export const INVITE_EXPIRY_DAYS =parseInt(process.env.INVITE_EXPIRY_DAYS || "5", 10);
export const INVITE_TOKEN_BYTES=parseInt(process.env.INVITE_TOKEN_BYTES || "32", 10);
export const FRONTEND_URL=process.env.FRONTEND_URL;
